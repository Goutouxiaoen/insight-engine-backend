# AGENTS.md — 后端仓库 AI 会话强制约束（自动加载，无需粘贴）

> 本文件被 AI 编码工具在每个新会话自动读取。**这里是硬约束，违反任何一条即任务失败。**
> 完整规范见 `docs/DEVGUIDE.md`；进度真相源见 `docs/PROGRESS.md`；跨端同步协议见 `docs/FE-SYNC.md`。
> 2026-09-09 因「前后端进展同步 gap」新增本文件：后端新对话不再依赖人工贴提示词，且必须主动读前端诉求。

## 铁律 0：开工四必读（缺一不可，读完先复述）

1. `docs/PROGRESS.md` —— 后端做到哪了、什么在阻塞（§五 / §七）
2. `docs/FE-SYNC.md` —— 面向前端的就绪信号与答复登记（§1 就绪清单 / §3 答复）
3. **前端仓库 `D:\JavaProject\insight-engine-web\docs\BE-ISSUES.md`** —— 前端提给后端的问题 / 诉求；`🟡 / 🔵` 条目本轮优先处理或明确排期
4. 涉及某模块时，读 `docs/IF.md` 对应章节 —— 接口契约唯一事实源

复述格式（5 行内）：后端当前阶段 / 本对话任务 / 前端待答复条目数 / 本任务相关模块就绪状态 / 有无阻塞。

## 铁律 1：前端诉求必答（跨端闭环）

- `BE-ISSUES.md` 中 `🟡 待后端答复` / `🔵 后端处理中` 的条目，**每轮对话开工先处理或明确给出排期**，不得静默跳过。
- 修复 / 确认后**双写**：① 前端 `BE-ISSUES.md` 该条目回填「后端答复 + 证据」并改状态 `✅`；② 本仓库 `docs/FE-SYNC.md` §3 登记同一答复；③ 若涉及契约变更，同步更新 `docs/IF.md`。
- 只允许写 `BE-ISSUES.md` 的「后端答复 / 证据 / 状态」字段，**不改前端其他任何文件**。

## 铁律 2：证据纪律（禁止虚假完成声明）

- 「已实现 / 实测通过 / code=0」必须附 curl 命令 + 响应摘要 + traceId + 日期；没验证的一律写「未验证」。
- 接口路径 / 字段 / 错误码只认 `IF.md` + 实测响应，不认记忆和推测。

## 铁律 3：范围与质量红线

- 只动 `d:/CodexProject/insight-engine/`；前端仓库只读（唯一例外见铁律 1 的双写）。
- 遵守 DEVGUIDE 附录 B 场景约束（P1~P19），不「顺手」重构。
- 后端没实现的接口，不得在文档 / 汇报中暗示前端可联调。

## 铁律 4：收尾三件套（每个会话结束必做）

1. 编译 / 冒烟通过（贴命令与结果）
2. 更新 `docs/PROGRESS.md`（进度 / 决策 / 踩坑 / 阻塞 / §八 摘要）；**模块状态变化必须同步 `docs/FE-SYNC.md` §1**
3. git commit（`feat(模块): …` / `fix(模块): …` / `docs: …`）；按 GitHub Flow 走 PR，**不直推 master**

## 铁律 5：凭据纪律（禁止明文入库）

- **禁止**把任何口令 / 密钥 / Token / 连接串（含「地址 + 口令」组合）写入**受版本控制的文件**——`application*.yml`、`docker-compose.yml`、`init.sql`、脚本、测试、文档、注释、commit message 一律适用。
- 配置只写**占位符 + 外部注入**（如 `${INSIGHT_PG_PASSWORD}` / `${POSTGRES_PASSWORD}`）；真实值只放 `.env`（`.gitignore` 已忽略）或环境变量 / secrets。**`.env` 永不入库**，仓库内只留 `.env.example` 模板。
- 新增配置项必须**同时**做三件事：① 代码写占位符；② 补 `.env.example`；③ 在文档写明变量名。缺一即视为未完成。
- 提交前自检：`git grep -niE "(password|passwd|secret|token|apikey)" -- ':!*.md' ':!*.example' ':!.gitignore'` 逐条确认有无明文，命中即**先报告再处理**，不得静默保留。
- 历史已泄露的凭据**按已泄露处理**：改文件 / 改文档只减少新增暴露面，**唯一止血是轮换口令**（见 `docs/PROGRESS.md` §五）。
- 不得以「本地联调方便」「先跑通再说」为由破例。

## 一句话版本

> **先读四份（PROGRESS / FE-SYNC / BE-ISSUES / IF）再动手；前端诉求必答必回填；说"实测"必给证据；模块状态变了同步 FE-SYNC；凭据一律占位符、明文永不入库；收尾必 commit 走 PR。**
